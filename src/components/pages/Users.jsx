import styled from "styled-components";
export const User = () => {
  return (
    <SContainer>
      <h2>Userページ</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
